# Improvement Backlog — moved

This file's contents moved to **[`OPEN_ITEMS.md`](OPEN_ITEMS.md)** on
2026-08-03, at the user's request: system improvements and portfolio
questions used to live in two separate places, which meant checking two
files to see what was outstanding.

They are now one list:

- **P-items** — portfolio questions (formerly
  `data/portfolio.json.open_structural_questions`)
- **S-items** — system improvements (formerly this file)

The rules are unchanged: the `meta` agent proposes S-items, nothing
self-applies, and you approve one by saying **"apply S3"**.

This stub is kept rather than deleted so that older session-log entries and
memos referencing `IMPROVEMENTS.md` still lead somewhere.
